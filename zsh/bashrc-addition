###############################

if [ -f "${HOME}/.localenv" ]; then
  source "${HOME}/.localenv"
fi

if [ -f /bin/zsh ]; then
  case $- in
  *i*) exec /bin/zsh ;;
  esac
fi

###############################
